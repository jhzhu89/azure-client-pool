# Azure Authentication Configuration
AZURE_AUTH_MODE=application
AZURE_CLIENT_ID=your-client-id
AZURE_TENANT_ID=your-tenant-id

# Optional: Certificate-based authentication (alternative to client secret)
# AZURE_CLIENT_CERTIFICATE_PATH=/path/to/certificate.pem
# AZURE_CLIENT_CERTIFICATE_PASSWORD=certificate-password

# Cache Configuration
CACHE_KEY_PREFIX=client

# Client Cache Settings (optional - defaults will be used if not set)
# CACHE_CLIENT_SLIDING_TTL=7200000  # 2 hours in milliseconds (default)
# CACHE_CLIENT_MAX_SIZE=50          # Default max size

# Credential Cache Settings (optional - defaults will be used if not set)
# CACHE_CREDENTIAL_SLIDING_TTL=14400000  # 4 hours in milliseconds (default)
# CACHE_CREDENTIAL_MAX_SIZE=10           # Default max size
# CACHE_CREDENTIAL_ABSOLUTE_TTL=43200000  # 12 hours in milliseconds (default)

# Environment Configuration
NODE_ENV=development
